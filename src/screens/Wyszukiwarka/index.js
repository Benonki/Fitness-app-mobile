import {Text, View} from "react-native";

const WyszukiwarkaScreen = ({ navigation }) => {
    return (
        <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
            <Text>WyszukiwarkaScreen</Text>
        </View>
    );
}

export default WyszukiwarkaScreen;
